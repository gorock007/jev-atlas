# Add social cards (Open Graph + X) so shared links render a preview
worker: claude
model: sonnet
effort: medium
started: 2026-09-18 16:51 · terminal: 73bdc9f4-5678-4f3a-bebb-5e81fee61503

## Goal
A link to any page of https://jev-atlas.vercel.app posted on X renders a large
summary card. Done = the built HTML of `/`, `/start`, `/map` and one record page
(`/ideas/agent-tool-firewall`) contains `og:title`, `og:description`, `og:image`,
`twitter:card=summary_large_image` and `twitter:image`, and the image URL
returns a 1200×630 PNG.

## Context
- Today there is no Open Graph config at all (`git grep openGraph` is empty).
- This is Next.js 16 with breaking changes: read the metadata and
  `opengraph-image` guides under `node_modules/next/dist/docs/` BEFORE coding.
- `metadataBase` must come from `process.env.NEXT_PUBLIC_SITE_URL` with the
  same localhost fallback `src/app/sitemap.ts` uses.
- Visual language: copy the tokens in `src/app/globals.css` — paper `#fffefb`,
  ink `#121212`, accent `#d62f12`, serif headline, mono uppercase label. The
  masthead is a large serif "J". No gradients, no emoji.
- Default card copy: label "JEV ATLAS · INDEPENDENT FIELD GUIDE", headline
  "What can you actually build with Jev?", footer "31 build blueprints · 9
  tracked claims · MCP for your coding agent". Read the counts from
  `loadAnalysis()` in `src/lib/research-data.ts`; do not hardcode them.
- Record pages (`/ideas/[slug]`, `/projects/[slug]`, `/patterns/[slug]`,
  `/claims/[slug]`) already export `generateMetadata` with title + description.
  Give them a per-record card: record title as headline, the record kind and
  evidence status as the label. Use `recordBySlug` from `src/lib/atlas.ts`.
  Long claim titles must truncate cleanly, never overflow.
- Must state "Not affiliated with TypeSafe AI" nowhere on the card — keep it
  clean — but never use TypeSafe's logo or imply official status.

## Scope
- May touch: new `src/app/opengraph-image.tsx`, new `src/app/twitter-image.tsx`
  (or the documented equivalent), new `opengraph-image.tsx` files inside the four
  `[slug]` route folders, a new shared helper `src/lib/social-card.tsx`, and
  a new test file `test/social.test.ts`.
- Must not touch: `src/app/page.tsx`, `src/app/globals.css`,
  `src/components/**` (another worker is editing the homepage right now),
  `data/**`, `research/**`, `package-lock.json`.
- Shared files (edit minimally, name in handoff): `src/app/layout.tsx` —
  only the `metadata` export (metadataBase, openGraph, twitter). Adding a
  dependency is not allowed; `next/og` ships with Next.

## Phases
1. Read the Next 16 docs for metadata files; settle the file convention.
2. Shared card renderer + default card + layout metadata.
3. Per-record cards for the four record kinds.
4. Verify against a production build (`npm run build && npx next start -p 3130`,
   curl the HTML and the image URLs, confirm PNG dimensions). Stop the server.

## Checks
`npm run check 2>&1 | tail -40` — report test count and build result. In the
handoff list the exact URLs the user should open, plus the card-validator step.

## Questions first
Put every question in one first handoff (`status: blocked`) and stop. After
the answers arrive on your terminal, build every phase through to done
without stopping again. If you have no questions, build straight through.
