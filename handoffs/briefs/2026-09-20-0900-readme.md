# Rewrite README.md for a first-time GitHub visitor
agent: claude · model: sonnet · effort: medium
Touch ONLY `README.md`. Target ≤ 70 lines, scannable in 30 seconds.

Order:
1. `# Jev Atlas` + one-sentence what-it-is, then a bold line: **Live: https://jev.gorock.sh**
   and the "Not affiliated with or endorsed by TypeSafe AI" note.
2. "What you can do with it" — 4 bullets, each with a live link:
   `/map` (31 build blueprints by lens), `/fit` (paste a workflow → Choice/Score/Noul
   decision points), `/claims` (every claim with an evidence status; vendor claims
   stay vendor claims), `/projects` (15 located builds).
3. "Connect your coding agent" — the MCP JSON snippet
   `{"mcpServers":{"jev-atlas":{"url":"https://jev.gorock.sh/mcp"}}}`, the five tool
   names in one line, and the no-MCP fallbacks (`/llms.txt`, `/llms-full.txt`,
   `/api/v1/*.json`). Keep the existing read-only guarantee sentence.
4. "Run it locally" — `npm install && npm run dev`; works with no keys. One line:
   the fit checker falls back to a rules result without `AI_GATEWAY_API_KEY`.
5. "Refresh the research (optional, costs X API credits)" — compress the current
   collection + budget-safety sections to ≤ 12 lines: the dry-run command, one
   `collect` example, and the three hard ceilings + 10% margin in one sentence.
6. "Data handling" — FIX the current inaccuracy: raw/processed posts, seen index,
   run state and `.env` are gitignored; the derived `data/processed/analysis.json`
   and `research/*.md` ARE committed and contain links, scores, categories and
   themes only — never post text (enforced by `test/security.test.ts`).
7. Stack one-liner (Next.js 16, React 19, Tailwind 4, MCP SDK v2, Vercel AI Gateway),
   Docs links (keep the two existing), License MIT.
Verify facts against the repo (`package.json`, `src/mcp/server.ts`, `.gitignore`)
— do not invent features. Plain language, no emoji, no badges. No checks needed
beyond proofreading; write the handoff per AGENTS.md.
