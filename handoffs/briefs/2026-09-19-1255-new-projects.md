# Add four newly located Jev projects to the project findings
agent: claude · model: sonnet · effort: medium

## Why
The 2026-09-19 collection surfaced four public links the atlas does not list.
Goal: visitors see real, inspectable things people built with Jev.

## Candidates (public URLs — read each README / page yourself with curl or WebFetch)
- https://github.com/gopalanj/chess-jev
- https://github.com/AshutoshVJTI/progressgate
- https://github.com/typesafe-ai/skills   (vendor-owned: say so in `builder` and `insight`)
- https://jevtypesafe.vercel.app/          (demo site; find its builder/repo if linked)

## Build
In `src/research/analyze.ts` → `deriveProjects`, add one `ProjectFinding` per
candidate, copying the exact shape and tone of the `HA-Jev` and `typesafe-mcp`
entries (one-sentence fields, arrows in `architecture`). Rules:
- `status: "ACTUALLY BUILT"` only if you opened the repo/demo and it contains
  working code or a working page. Otherwise use the weaker status already in the
  `ProjectFinding["status"]` union. If a link is dead or has nothing to do with
  TypeSafe's Jev, leave it out and say so in the handoff.
- Describe from the repo/page in your own words. **Never read `data/raw` or
  `data/processed/posts.jsonl`, and never quote X posts.**
- Do not claim performance, users, or accuracy the repo does not show.
- Update any test that pins the project count (`rg -n "projects" test`).

## May touch
`src/research/analyze.ts` (the `deriveProjects` array only), tests that pin counts.
## Must not touch
Everything else. Do NOT run `npm run analyze|report|research|collect|explore|expand`
— Jarvis regenerates `analysis.json` after merge. Another worker is building
`/fit`; stay out of `src/app`, `src/components`, `src/lib`, `src/mcp`.

## Done
`npm run typecheck && npm test 2>&1 | tail -15` green (skip `next build`: the other
worker shares `.next`). Handoff lists each candidate: added / skipped + why.
No questions expected — build straight through.
