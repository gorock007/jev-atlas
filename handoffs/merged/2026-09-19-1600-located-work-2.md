# Verify and add the second batch of located Jev work
status: done
agent: claude sonnet
brief: handoffs/briefs/2026-09-19-1520-located-work-2.md

## What the user asked
Verify a second batch of candidate Jev-related builds and links (by opening each one), add each verified build as a `ProjectFinding`, fold the chess-jev Hugging Face model into the existing chess-jev entry, and report on three third-party writing/platform-support links without editing code.

## What changed
Added five new `ProjectFinding` entries to `deriveProjects` in `src/research/analyze.ts`, folded a Hugging Face model into the existing `chess-jev` entry, and left the three writing/platform links for Jarvis to place (summaries below).

### Builds added
- **https://github.com/gargpratyush/jev-router — added.** Real, working repo (pushed 2026-09-18): CLI wrapper launching Claude Code / OpenAI Codex through a loopback proxy, with a documented test suite. One Jev call per turn picks an abstract tier (fast/balanced/strong/long); a local deterministic policy (`src/policy.mjs`) applies overrides, fail-open on error, and no-downgrade-at-low-confidence rules before choosing the concrete model.
- **https://github.com/TheoLeeCJ/openjev and https://openjev.com — added as "SemIf (formerly OpenJev)."** The GitHub repo redirects/renamed to `TheoLeeCJ/SemIf`; both the README and the live site state, unprompted, "Independent research project... not affiliated with or endorsed by TypeSafe." It is an **independent reimplementation of the interface pattern** (typed options read as native logits from a locally hosted open model, e.g. Qwen3.5-4B on a home 3090) — it does not call TypeSafe's Jev API and explicitly disclaims being Jev. Described plainly as such per the brief's instruction, with no implication it's TypeSafe's product.
- **https://j3vroute.agents.bakingbad.dev — added as "J3vRoute."** Live single-page app (client-rendered; verified by pulling the JS bundle, not just the HTML shell). Real working paper-trading demo: "Robinhood Chain" three-market virtual USDG portfolio, fills explicitly labelled simulated/dry-run. Jev picks buy/sell/hold per market and sizes trades via a Score. Paired with a routing protocol called 3Route. No linked GitHub repo or builder identity found anywhere in the bundle, so `builder` is "Unknown (no linked repo)" and `repositoryOrDemo` is `null`, per the prior handoff's fallback convention.
- **https://www.npmjs.com/package/system-one-router — added.** Real published npm package, v0.4.5, for the Pi coding agent. README states plainly it's based on and retains attribution to an existing router (`yeliu84/pi-model-router`); Jev is one optional LLM-intent-classifier backend (model `jev-latest`) behind the package's own heuristics, which remain the default. No repo link in registry metadata, so `repositoryOrDemo` is `null`.
- **https://www.npmjs.com/package/is-odd-jev — added, marked as a joke package** in `insight` per the brief. Real published npm package, v1.1.0, zero dependencies. It genuinely calls Jev's Noul primitive (via `api.typesafe.ai` or the Vercel AI Gateway's `typesafe-ai/jev`) and returns a calibrated probability rather than a bare boolean — a real, if deliberately silly, integration in the `is-odd`/`is-odd-ai` lineage.
- **https://huggingface.co/gopalanj/chessjev-minilm-v1 — folded into the existing `chess-jev` entry** (`description` and `jevRole` fields), not added as a new entry, per the brief. It's a locally trained MiniLM-L6 + attention-head model (Apple M1 MPS, Stockfish 19 teacher labels) whose own model card says "This is not TypeSafe Jev. It is not Elo-tested" — consistent with the existing entry's framing of Jev as one optional hosted backend behind local defaults.

No candidates were skipped as dead/unrelated — all six build-shaped links were live and verifiably real.

### Writing / platform-support links (not added as projects — for Jarvis to place)

**https://www.langchain.com/blog/building-a-harness-with-jev** — A LangChain-published blog post (authored by LangChain team members Sydney Runkle and Hunter Lovell) explaining Jev as a "System One model" and covering its integration into LangChain's own tools for model routing and safety guardrails. This is **vendor-adjacent, not independent**: LangChain is promoting a direct product integration with TypeSafe, so it demonstrates a real platform integration but should not be read as independent third-party adoption evidence. Relevant to the "Developers have published small Jev integrations and demonstrations" claim in `research/claims.md` — but as a platform partnership, not grassroots adoption.

**https://openrouter.ai/api/alpha/decisions** — A bare GET to this URL 404s (it's a POST-only alpha endpoint), but web search confirms it's real: OpenRouter has published a Decisions endpoint (`typesafe/jev-latest`) that accepts Jev's native `{model, state, questions}` body, letting anyone with an OpenRouter account call Jev without a separate TypeSafe key. Multiple independent third-party repos (issues/PRs in unrelated projects) reference wiring their own Jev integrations through this endpoint. This is **platform-support infrastructure, not a project**, but it's good evidence for the "Developers have published small Jev integrations" claim and for the "Headline benchmark and reliability claims remain insufficiently independently verified" claim's counterpoint — availability without a TypeSafe account lowers the bar for independent testing.

**https://www.theregister.com/ai-and-ml/2026/09/16/typesafe-ai-debuts-model-for-machines-that-plays-doom/5296711** — Independent trade-press coverage (The Register) of TypeSafe's Jev launch: describes it as a machine-to-machine model producing typed probabilistic decisions, repeats TypeSafe's own speed (70-500ms, "40x-200x faster") and price ($0.042/M input tokens) claims, and notes it can play Doom from structured game state. Notably, the piece pushes back on its own: it calls the "hallucination-free" framing "really isn't a fair comparison" and questions whether real-world token-consumption growth will match TypeSafe's prediction. This is genuinely independent journalism with built-in skepticism, so it's strong evidence for the "Headline benchmark and reliability claims remain insufficiently independently verified" claim — it's press coverage that relays and lightly interrogates vendor claims, not independent benchmarking.

## Files
- src/research/analyze.ts — modified (added five `ProjectFinding` entries to `deriveProjects`, before the `PROPOSED` entry; extended the existing `chess-jev` entry's `description` and `jevRole` with the Hugging Face model)

## Checks run
- `npm run typecheck` — clean, no errors.
- `npm test 2>&1 | tail -8` — 32/32 tests pass, 0 fail.
- `next build` not run (per brief scope; shared `.next` across workers).
- Did not read `data/raw` or `data/processed/posts.jsonl`; no X post text quoted anywhere.

## Notes for Jarvis
- `data/processed/analysis.json` and `research/*.md` still need regenerating (`npm run analyze`/`report`/`research`) to pick up these changes — reserved for you.
- The three writing/platform-support summaries above are for you to place (likely `research/claims.md` evidence lists and/or a short new section — your call, per the brief).
- Suggested commit message: `feat(research): add jev-router, SemIf, J3vRoute, system-one-router, and is-odd-jev to project findings`
- Touched only `src/research/analyze.ts` (`deriveProjects` array), as scoped.
