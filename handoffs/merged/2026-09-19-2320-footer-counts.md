# Footer counts derived from the analysis
status: done
agent: claude fable
brief: handoffs/briefs/2026-09-19-2240-visual-wayfinding.md (follow-up sent over the terminal)

## What the user asked
The footer in `src/app/layout.tsx` hard-coded "165 posts · 9 claims · 31 hypotheses". Derive the three numbers from the same source as the homepage corpus numbers; no other layout change.

## What changed
`RootLayout` is now async and reads `loadAnalysis()` — the same call the homepage uses. The footer shows `dataset.totalPosts` posts, `claims.length` claims, `ideas.length` hypotheses, the exact fields behind the homepage's "posts collected", "claims tracked" and "build blueprints". With the current data that reads "431 posts · 9 claims · 31 hypotheses". If the analysis file is missing the middle paragraph renders empty, so the three-column footer grid keeps its shape. Markup, classes and everything else in the layout are unchanged.

## Files
- src/app/layout.tsx — modified (shared)

## Checks run
- `npm run check`: typecheck clean, 39/39 tests pass, production build succeeds.
- Confirmed in the built HTML for `/` and `/claims`: `<p>431 posts · 9 claims · 31 hypotheses</p>`. `/` is still prerendered static (`○`), so the async layout did not make pages dynamic.
- Not checked: no browser look — the change is text-only inside an existing element.

## Notes for Jarvis
- Shared-file edit: `src/app/layout.tsx` only (one import, `async`, one line of footer text).
- "posts" uses `totalPosts` (431, unique posts collected), matching the homepage's first corpus figure, not `retainedPosts` (155).
- Suggested commit: `fix: derive footer counts from the analysis`
