# Add four newly located Jev projects to the project findings
status: done
agent: claude sonnet
brief: handoffs/briefs/2026-09-19-1255-new-projects.md

## What the user asked
Add four newly located public links as `ProjectFinding` entries in `deriveProjects`, verifying each by reading the actual repo/page content, using `ACTUALLY BUILT` only where working code/page was confirmed.

## What changed
Added four entries to `deriveProjects` in `src/research/analyze.ts`, all `ACTUALLY BUILT`:

- **https://github.com/gopalanj/chess-jev — added.** Real working code: FastAPI/uvicorn server, browser UI, 19 passing unit tests, trained local checkpoints. Jev is wired in as one optional hosted scoring backend (`CHESS_JEV_SCORER=typesafe`, posts to `api.typesafe.ai`, model `jev-latest`) behind locally trained defaults. The README explicitly disclaims being "TypeSafe Jev" or "OpenJev," so I described it accurately as Jev-as-optional-backend rather than a Jev-native build.
- **https://github.com/AshutoshVJTI/progressgate — added.** Directly built on Jev's `systemOne` API (published to npm, real source in `src/sdk/jev.ts`, `src/sdk/policy.ts`, tests, quickstart, demo build script). Clean fit.
- **https://github.com/typesafe-ai/skills — added, marked vendor-owned** in both `builder` ("TypeSafe (vendor)") and `insight` (says it's vendor-authored and shows the vendor's own recommended pattern, not independent adoption evidence), as instructed.
- **https://jevtypesafe.vercel.app/ — added.** Confirmed by fetching the raw page: it's a working interactive demo (Support Router / Compare & Choose / Content Triage use cases, real form + Jev-backed analysis). I found no linked GitHub repo or builder identity anywhere in the page (footer has only a request counter, no links), so `builder` is "Unknown (no linked repo)" and `repositoryOrDemo` is `null`, per the brief's fallback rule.

No candidates were skipped — all four were live, on-topic, and showed real working code or a real working page.

## Files
- src/research/analyze.ts — modified (added four `ProjectFinding` entries to `deriveProjects`, before the existing `PROPOSED` entry)

## Checks run
- `npm run typecheck` — clean, no errors.
- `npm test 2>&1 | tail -25` — 24/24 tests pass, 0 fail. No test asserts an exact project count, so nothing needed updating.
- `next build` skipped per brief (another worker shares `.next`).

## Notes for Jarvis
- `data/processed/analysis.json` and `research/*.md` still need regenerating (`npm run analyze`/`report`/`research`) to pick up these new entries — that's reserved for you per AGENTS.md.
- Suggested commit message: `feat(research): add chess-jev, progressgate, typesafe-ai/skills, and Jev Playground to project findings`
- Touched only `src/research/analyze.ts` (the `deriveProjects` array), as scoped.
