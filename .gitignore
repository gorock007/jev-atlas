node_modules/
.next/
*.tsbuildinfo
.env
.DS_Store
coverage/
*.log

# Collected X content is local by default: it may contain licensed Post text.
# data/processed/analysis.json is deliberately NOT ignored — it carries only
# authored analysis and derived signals, and the deployed site is built from it.
data/raw/*.jsonl
data/processed/*.jsonl
data/seen-posts.json
data/*run-state.json
!data/raw/.gitkeep
!data/processed/.gitkeep
.vercel
.env*
!.env.example
